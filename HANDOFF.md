# HANDOFF.md（現在地と次の一手）
最終更新: 2026-08-15

## 【この文書の更新ルール】
- 更新は**全文差し替えのみ**。部分追記・差分パッチでの受け渡しは禁止。
- Claudeが更新版を出す時は、必ずこの冒頭ブロックと下の「収録セクション一覧」を含めた
  全文を出力する。一部だけを出してはならない。
- TAKAは貼る前に、出力の頭に「収録セクション一覧」があるかを確認する。
  無ければ不完全版＝貼らない。あっても本文の見出し数と一致しなければ貼らない。
- セクションを増減したら、一覧の本数も必ず更新する。数のズレは異常の合図。
- 細かい宿題リスト・実装詳細・観測記録はここに盛らず、テーマ別docs/MDへ逃がし、
  ここには「詳細は○○.md参照」の1行だけ置く（運用ルール⑨）。
- **本ファイルの分割が保留課題**（2026-08-04起票）。現在77KB超。
  「今やること」と「終わったことの記録」が同居して読む負荷が高い。
  バグ・観測記録をdocs/へ逃がしてHANDOFF本体を1/4程度に絞る案。別チャットで実施

## 収録セクション一覧（全14本）
1. 今どこ
2. 次の一手
3. ドメイン
4. サーバー構成とメール
5. ローンチ前タスク
6. 印刷カード裏面
7. mypage整理ボタン
8. mypage実機宿題
9. 新規確定事項
10. DB観測メモ
11. 保存確定カード
12. 検討中テーマ
13. リポジトリ内ファイル
14. 書いたらあかんもの

## 今どこ
- セキュリティ監査: 第1次（2026-07-18〜19・22項目）＋第2次（2026-07-20・
  RPCのEXECUTE権限×内部ガード層）完了。第2次でcreate_bundle系のanon直叩き穴を
  発見→同日修正済み。詳細はdocs/監査マトリクス（7/20更新版）
- **第2次監査の取りこぼしを発見・修正（2026-08-04）**。create_bundle_v2 は
  7/20にanonを剥がしたが**PUBLIC権限が残っており剥奪が打ち消されていた**。
  同日 revoke 実施・proaclで消滅を確認。詳細は「新規確定事項」
- 適用済み: profiles対策一式（権限整理＋自動作成トリガー・配線確認済・
  2026-07-19 DB検証でanon/authenticatedはSELECTのみ確認）／
  room_posts DELETEポリシー／discard_card整備版／claimed_at埋め戻し／
  RPC権限最小化（2026-07-20: create_bundle v1封鎖・v2 anon剥奪・
  内部専用2本剥奪・delete_room等のanon剥奪）／
  レガシーposts緩いINSERTポリシー撤去（旧・軽微件①を前倒し消化）／
  **create_bundle_v2 のPUBLIC剥奪（2026-08-04）**
- docs/整理: 正MD7枚アップ済み（監査マトリクス／電気通信事業法判断地図／
  規約プラポリ骨子／印刷エンジン実行指示書v1.3／大掃除リスト／
  rlsautotest実行指示書／言葉ストック）・NGスキャン通過
- 印刷エンジン: 実行指示書v1.3確定（400枚バッチ×5回・4丁付け・両面・
  束100=山=1bundle・合紙不要）。裏面レイアウト追記済（2026-07-24）。
  冠野K1/K3/K4の返信待ち
- 冠野栄興社: 確認メール送信済み（2026-07-20）・返信待ち
- ドメイン: **接続完了 heartcloak.com（2026-07-29）**。
  GitHub Pages接続・HTTPS有効化まで完了。Supabase Auth も新ドメインへ切替済み。
  旧github.ioのカードはリダイレクトで生存＝刷り直し不要（実機確認済み）。
  作業記録と積み残しは docs/ドメイン接続記録_2026-07-28.md
- **メール構築 完了（2026-08-08〜08-12）**。契約＝**さくらのメールボックス**
  （12ヶ月一括1,320円／初期費用0）。当初「スタンダード」想定だったが、
  Webは GitHub Pages で足りておりメール専用で十分なため下位プランに変更。
  受信・送信・システム送信の3種すべてさくらで稼働中。Resendは予備のまま。
  DNS4件（MX/SPF/DKIM/DMARC）反映・外部検証済み。Supabase Custom SMTP接続済み。
  実メール到達をGmail受信トレイで確認（迷惑メール判定なし）。
  詳細は下記「サーバー構成とメール」「ローンチ前タスク」
- 法務: 現状（収益ゼロ）は届出不要の公算。協賛収益開始の直前に届出。
  個人名義で届出するか法人化してからかの判断が未了＝docs/判断地図に追記済（2026-07-24）
- Netlify: 削除完了（2026-07-22）
- **家庭用カード: 両面へ変更・試作を実地確認（2026-08-02）**。
  ロットH0002をコンビニでカラー両面印刷。両面の重なり正常（長辺とじ）・
  見当ズレは目視ほぼゼロ。QR仕様は家庭用16mm/ECC Q、印刷屋22mmで系統分け確定。
  **家庭用16mmのQR読取は実運用でも成功を確認（2026-08-04・受け手側の実機）**。
  print.html は片面のままで**両面化の改修が未着手**（回避策で先行検証した）。
  詳細は docs/家庭用カード仕様_2026-07-29.md
- **名称確定: HeartCloak Project ／ こころづて（2026-08-02）**。
  「お互い様プロジェクト」はプロジェクト名としては廃止（同名の他事例が存在するため）。
  以後は表題・文中の一表現としてのみ使う。
  表示ルール＝日本語「こころづて」／英語「HeartCloak Project」（半角スペース区切り）。
  本番HTML13ファイルの表示文言を差し替え済み（title／og／ヘッダ／フッタ／フォーム件名）。
  index.html に meta description・og:title/description/url を新規追加。
  「スポンサー」表記は表示文字をすべて「協賛」に統一。
  **部屋の呼称を「感謝の部屋」→「クローク」に変更（2026-08-02）**。理由＝感謝に
  限定しない器にするため。HeartCloak の cloak（預かり所）と直結し、階層も
  「HeartCloak Project（ブランド）／クローク（画面の種類）／ROOM #番号（個体）」で通る。
  英字を添えるなら CLOAK。**表示に英字だけを使うのは不採用**（和名を主役にする組み方に反する）。
  **「QR」表記はモノを指す箇所を「名刺」に変更**（受け手にとって渡されたのは名刺で、
  QRはその上の部品）。読み取る動作を指す「QRコード」はそのまま残す。
  **未対応の残り3点**（2026-08-02時点）:
  ①print.html のカード面（520/656/662行）＝フォント再生成が前提のため据え置き。
    ※fonts_subset.js は**カード文言の文字だけを収録したサブセット**で、文言を変えると
      豆腐になる。実測で日本語26字が不足。さらに **`β` は Shippori Mincho に収録が無い**
      （ギリシャ文字）ためラテン用の別フォント併用が必須
    ※2026-08-03に実機確認：print.html の表示は「OTAGAISAMA PROJECT／お互い様プロジェクト」
      のまま。既知どおりで異常ではない
  ②CSSクラス名 .sponsor-*（index/receive）＝内部識別子。表示・動作・検索いずれにも
    影響なし。変えるならHTML側とCSS側を同時に。優先度低
  ③docs/配下MDの旧表記＝公開リポジトリでrawが読める。優先度低
- **本番HTML6ファイルを差し替え済み（2026-08-02）**。card / mypage / auth /
  index / receive / print。内容＝画像アップロード不具合の修正、ペンネーム保存、
  お役目終了後に中身が隠れるバグ修正、URL部屋の削除導線、クローク化、
  QR→名刺の表記整理、アイコン宣言。全ファイルraw照合で一致を確認済み
- **【2026-08-07・印刷前ブロッカーを5件まとめて解消】**。
  「紙に焼いたら直せないか」「配った直後に人が踏むか」の2軸で並べ替え、
  印刷を締め切りとして上流から潰した。以下すべて実機・md5・SQLで裏取り済み。
  ①**アイコン4点のファイル名を修正**（正しい名前で再アップ・壊れた名前は削除済み。
    raw で4本とも200/26,958バイトを確認）
  ②**card.html の catch にログを追加**（1181行 `部屋作成エラー` ／
    1191行 `部屋生成エラー`。例外の握り潰しを解消）
  ③**short_id の紛らわしい文字を除外**（次の一手17へ移動・完了記録）
  ④**lot_no 引き継ぎを3関数に実装**（次の一手11・完了）
  ⑤**G666表示を修正**（次の一手14・完了）
  ⑥**print.html の白紙＝既読を解消**（次の一手16・完了）
  ※①②は「今どこ」内の旧記述を置き換え済み。詳細は各「次の一手」項目参照
- **アクセスログ棚卸しを実施（2026-08-03〜07）**。card_access_log を全期間で読み、
  最終的に receive.html のコードまで確認した。詳細は「mypage実機宿題／ログ機能」。
  ①**G666バグの原因と該当カードが確定**→**2026-08-07に修正完了**（次の一手14）
  ②**lot_no がバトンで引き継がれないことがコードで確定**→**2026-08-07に修正完了**（次の一手11）
  ③**card_access_log の壊れ方が判明**（次の一手12へ）。
    **2026-08-07にコードを読み、当初の仮説「未ログイン経路が抜けている」は否定された**
  ④global_no二重に **#68** を追加発見（既知は69/71/72）
  ⑤「自分が見たか／相手が見たか」を判定する保存クエリが実用段階に到達
  ⑥全RPC18本の proacl を棚卸し（次の一手15へ）
  ⑦**print.html の白紙表示が既読機能になっているという指摘**→**2026-08-07に解消**（次の一手16）
- **設計の複雑さではなく「作り足した順番」が事故の原因という整理（2026-08-04）**。
  今回まとめて出た問題は、G666／白紙／二重カウント／global_no二重／5桁short_id／
  pen_name null と多く見えるが、**原因はほぼ一つの型に収束する**＝
  「後から仕組みを足したとき、それを参照する側を全部直していない」。
  G666と白紙は完全に同一原因（世代交代でshort_idが移ることを card.html と
  print.html が知らない）。
  **事故るのは「同じ情報の在処が2つある」箇所**:
  ・short_id が旧部屋/新部屋の2状態を持つ → G666・白紙
  ・ログ書き込みが receive/card の2箇所 → 二重カウント
  ・pen_name が room_posts/rooms の2箇所 → 匿名表示
  ・番号が global_no/lot_no の2系統 → 引き継ぎ漏れ
  → **新しい仕組みを足すときは「これを読む側は他にどこか」を必ず洗う**。
  設計を単純に保つ努力とは別軸の、手順の問題として扱う
  ※**2026-08-07に3件、2026-08-15に二重カウントを解消し、4件すべて決着した**
- **推測で原因を決めない（2026-08-07の教訓・2件）**。
  ①ログの取りこぼしについて、外から状況証拠だけで「QR型の未ログイン経路が
    抜けている」と仮説を立てたが、receive.html を読んだら**その分岐は存在しなかった**。
    サンプル2件で構造を断定していた。**コードを読めば10分で済む話に何日もかけた**
  ②**G666の原因説明が間違っていた**。HANDOFFには「claim の前後に short_id 検索が
    空を返す」と書いていたが、reply_and_claim の UPDATE と INSERT は同一
    トランザクション内のため、外から見て「どちらも持っていない瞬間」は原理的に無い。
    **真因は RLS**。rooms の SELECT ポリシーは3本とも自分の部屋限定
    （`auth.uid() = owner_user_id` ／ `= user_id` ／ `is_operator()`）で、
    **匿名に開かれたポリシーが1本も無い**。行は存在するのに見えていないだけだった
  → 状態遷移・権限・分岐が絡む話は、裏取り原則（運用ルール⑧）どおり
  最初にコードを読むほうが速い
- **【2026-08-15・ログ機能の修正と、その過程で確定した事実】**。
  ①**次の一手12を完了**（二重カウント解消・エラー握り潰し解消）。実機で1件のみを確認
  ②**「Androidが古いiPhoneと表示される」の正体が判明。誤判定ではなかった**。
    保存クエリのCASEは Android より iPhone を先に評価していたが、
    **AndroidとiPhoneを両方含むUAは全期間で0件**（SQLで確認）＝誤判定は発生しえない。
    真相は **iOS 18_7 の実在する別端末**。TAKAの端末は 26_5_2（8/12に 26_6_0 へ更新）で、
    そこだけOSが古かった。CASEの並べ替え自体は将来の予防として実施済み
  ③**保存クエリ「アクセス」に iOS 列を追加**（`substring(l.ua from 'iPhone OS ([0-9_]+)')`）。
    これが決定打で、**自分の端末と外来端末が一目で分離できる**ようになった
  ④**全期間の外来アクセスは3件のみ**と確定:
    8/03 06:45（iOS 18_7・LINE・未ログイン）／
    8/07 14:24〜15:02（Android・LINE・12回。14:48にログイン成功）／
    7/20 23:12（Android・未ログイン）。
    **7/22〜7/23の「相手（登録者）」は全て 26_5_2 ＝TAKA自身の端末**だった
  ⑤**auth.html のパスワード忘れ導線は実装済みと確認**（新規実装は不要）。
    ログインフォーム内にボタン・`resetPasswordForEmail`・`PASSWORD_RECOVERY` 検知・
    `updateUser` まで完備。**メアドの存在有無を漏らさない作り**になっており、
    redirectTo も heartcloak.com（旧ドメインではない）。
    ※TOPページに置く案は不採用。まだ登録していない人に「パスワード」の語を
      見せることになり、受け取りの入口の空気を濁すため
  ⑥**8/11のパスワード再発行は n.borderless（TAKA本人）の文言確認テスト**だった。
    `auth.users.recovery_sent_at` を全件確認し、korokoro アカウントは **null**＝
    一度も再発行していないことが確定。次の一手6の「妹のログイン待機症状」を
    パスワード再発行と結びつける仮説は成り立たない
- rlsautotest（RLSの自動実測検証・Supabaseニュースレター発）: 導入検証済み。
  公開前チェックの任意候補。本番直当て禁止＝使い捨てコピー必須。
  指示書はdocs/rlsautotest実行指示書_2026-07-19.md
- HANDOFF読込の注意: raw素読みはキャッシュで古い版が返ることがある。
  ?cb=タイムスタンプ付きで取得（Claudeメモリにルール化済み）。
  読んだ時点でmd5を控えておくと、更新有無を後から厳密に照合できる
  ※**2026-08-15に実際に事故った**。プロジェクト指示にURLだけ書いてあったため
    Claudeが web_fetch で素読みし、**7/14版（当時1ヶ月前）を掴んだまま会話を進めた**。
    web_fetch にはキャッシュ回避手段が無い。**プロジェクト指示を「bash_toolで
    cb付きcurlを実行」と明記する形に書き換え済み**（2026-08-15・TAKA実施）。
    複数チャットで同じ事故が起きていた可能性が高い

## 次の一手
1. ✅**完了（2026-08-12）**: ドメイン残タスク。
   ドメインプロテクションON（**無料**・認証用メールは個人Gmailのまま＝
   heartcloak.com自身に依存させない構成）。
   リマインダー2本を「プロジェクト」リストへ登録済み（iPhoneのClaudeアプリから）:
   heartcloak.com更新 2028-07-19通知（期限2028-07-26）／
   **さくら更新 2027-07-10通知（＝次回請求日。2026-08-12に 2027-08-01 から変更）**。
   さくらは満期の約1ヶ月前に請求が立つため、満期日ではなく請求日に鳴らす。
   決済カードの有効期限切れで落ちる事故を、請求が通る前に拾うのが狙い。
   **残は更新猶予・復旧手数料のXserver実運用確認のみ**（購入後TODO(4)）
2. 冠野返信きたら → K1/K3/K4を指示書v1.3に記入
   → 実装チャット（Sonnet／Claude Code）へ渡して印刷エンジン実装
3. 実装後: 第1バッチPDF検品（QR読取・番号ズレ）→ 発注
4. mypage: 整理ボタン＋UI調整5件 2026-07-22実装・差し替え反映確認済み。
   実機動作の最終確認のみ残（下記「mypage整理ボタン」参照）。
   実機で出た追加宿題は「mypage実機宿題」セクションへ集約。
5. 決定台帳MDの作成（起票2026-07-22・テーマ軸で1本ずつ積む）:
   「こう決めたからこうなってる」の設計判断ログをdocs/決定台帳.mdに集約。
   開発の拠り所＋ユーザー説明のタネ。第1章は番号体系（H接頭辞・0001-047）から。
   第2章はドメイン選定（2026-07-24〜26の判断）を充てる。
   **第3章の候補＝ポイント重み付け1:2の根拠**（2026-08-07・下記「新規確定事項」）
   ※内部戦略（協賛の値付け・心の二層構造の設計意図・無名の神枠・営業フレーム）は
     公開リポジトリに置かない＝リポジトリ外で別管理。章分けでなくファイル/置き場所で分離
6. ✅**完了（2026-08-08〜08-12）**: メール構築。
   さくらのメールボックス契約→DNS4件→Supabase Custom SMTP→Web3Forms転送先変更まで
   一気通貫で完了。実績値と設定内容は「ローンチ前タスク」に集約。
   **ネームサーバーの食い違いはA案（Xserverのまま）で決着**（2026-08-08）。
   決め手＝①動いているWeb設定（Aレコード4本＋www CNAME）に触らずに済む
   ②HANDOFFが「Gmailに落ちる兆候が出たらResendへ移す」と書いている＝
   さくらを差し替えうる部品として扱っている以上、そこにDNSを預けるのは筋が通らない。
   B案の利点（DKIM自動設定）は「失敗しても気づけて直せる軽い事故」の回避に過ぎず、
   NS移行の「サイトが落ちる重い事故」のリスクと釣り合わない。
   ※NSはいつでも切り替え可能＝一度きりの選択ではない。将来さくらへ寄せる余地は残る。
   **2026-08-07の妹のログイン待機症状は原因未確定のまま**。
   Custom SMTP未設定を疑ったが、後述のとおり**ログインではメールが飛ばない**
   （signInWithPassword）ため、Supabaseのメール制限では説明がつかない。
   ログイン試行回数の制限など別要因の可能性が高い。
   ※**2026-08-15にDBで裏取り**: korokoro アカウントの `recovery_sent_at` は **null**＝
     妹は一度もパスワード再発行を行っていない。再発行メールの不着が原因という筋は消えた。
     なお8/11の `recovery_sent_at`（n.borderless）はTAKA本人の文言確認テストで、
     **新SMTP経由の resetPasswordForEmail は実戦で1度通っている**
   **未検証で残るのは signUp のみ**（新規登録が2026-05以降ゼロ＝新SMTPで
   一度も走っていない）。**印刷・ベータ公開前の必須テスト**（下記ローンチ前タスク）
7. （任意・急がず）公開前チェックでrlsautotest実行（docs/指示書参照）
8. **【次の本丸】家庭用カード print.html の両面化改修**（2ページ化・裏面描画・
   ミラー・QR16mm・ECC Q・文言差し替え・フォントサブセット再生成）。
   仕様は docs/家庭用カード仕様_2026-07-29.md に確定済みで、実装チャットへ渡せる状態。
   **前提だった次の一手16（白紙＝既読）は2026-08-07に決着済み＝着手可能**。
   工数が大きいため別チャットで腰を据えてやる。次の一手10（名称差し替え）と同時処理
9. ✅**完了（2026-08-07）**: アイコン4点のファイル名を修正。
   壊れた名前（拡張子なし・空白入り）を削除し、favicon.ico / apple-touch-icon.png /
   icon-192.png / icon-512.png で再アップ。rawで4本とも200・26,958バイトを確認。
   ※4ファイルとも中身は同一画像（バイト数完全一致）。favicon.ico の中身はPNG。
     クウの絵ができるまでのつなぎと割り切って深追いしない判断（エスカレーション原則）。
     Safari等でfaviconが出ない可能性は残る
10. **print.html カード面の名称差し替え**（fonts_subset.js 再生成が前提）。
   単独でやらず、次の一手8の両面化改修と同時に処理する（どちらもサブセット再生成を伴うため）
11. ✅**完了（2026-08-07）**: reply_and_claim 等が lot_no を引き継いでいなかった件。
   **修正した関数は3本**（当初は reply_and_claim 1本と見ていたが、
   コードを読んで claim_room・reset_blank_and_claim も同じ欠落があると判明）。
   各関数の新部屋 INSERT の列リスト末尾に `lot_no`、VALUES 末尾に
   `v_room.lot_no`／`v_old.lot_no` を追加。pg_get_functiondef で3本とも反映を確認済み。
   **既存の実害1件も修復**: #267（id287・紙はH0002-002）に lot_no を埋め戻した。
   ※**修復対象は1件だけだった**。lot_no が NULL の部屋は他に29件あるが、
     いずれも lot_no 列を作る前（7月上旬）のテスト残骸で紙が存在せず、
     埋めるべき値も無い＝正しいNULL。放置でよい
   ※**旧部屋は lot_no を保持したまま**（NULL化されるのは short_id だけ）。
     この事実が次の一手16の解決にも効いた
12. ✅**完了（2026-08-15）**: card_access_log の記録を直した。
   **修正2箇所**:
   ・**card.html 1269行を削除**（`log_card_access` の呼び出し）。
     receive.html が `is_owner` 判定後に card.html へ `location.href` で飛ぶため、
     同一アクセスが2件記録されていた（0.4秒差ペア）。receive 側だけを残す
   ・**receive.html 361行を差し替え**。`.then(null, () => {})` を
     console.error 付きに変更。supabase-js v2 は HTTPエラーで reject しないため、
     `error` フィールドの明示チェックと例外ハンドラの両方を設置
   差し替え後md5: card.html `05fd34c44b9fb2a385e4bd9318c29f46` ／
   receive.html `f4f0ff1b9a000868a87b7b74a6997869`（rawで照合済み）
   **実機確認（2026-08-15 05:58）**: #265（7d7hfs）を receive URL 経由で開き
   card.html へ遷移 → ログ **1件のみ**。二重カウントの解消を確認
   ※**開封数を協賛先やユーザーに公表する予定は無い**（ログは保全・裏方に限る原則）。
     直した理由は運営が状態を正しく把握するため
   **残る既知の欠陥**:
   ・**取りこぼしは未解決**。#256（4yoftg）は受け手が開いたことが確実だが記録が無い。
     console.error を入れたので**今後は失敗が見える**が、過去分は復旧不可
   ・**1人が複数件を出すのは仕様どおり**。開くたびに `loadCard()` が走り1件入る。
     **ログから「何人が開いたか」は数えられない**。数えられるのはアクセス回数のみ
   ・IP列は無い（取得の可否は思想判断が先。詳細は docs/運営ダッシュボード）
13. **アクセス履歴を定期的に見る運用**（2026-08-02起票／2026-08-12にdocsへ集約）。
   Supabase SQL Editor の保存クエリ2本（★日別サマリ／アクセス明細）で回す段階。
   ビュー化・admin.html埋め込みは**印刷の後**。日次通知は作らない（判断済み）。
   → **詳細は docs/運営ダッシュボード_2026-08-12.md**
   ※**2026-08-15に保存クエリ「アクセス」を改訂**（端末判定の順序修正・iOS列追加・
     LINE内ブラウザ列追加）。**docs/運営ダッシュボードへの反映が未了**＝次に
     あのdocsを触るときに書き戻すこと
14. ✅**完了（2026-08-07）**: 「G666」表示の修正。
   **真因は RLS だった**（HANDOFF旧記述の「claim前後の隙間」は誤り。上記「今どこ」参照）。
   card.html は rooms を直に select しているため、他人の部屋・未ログイン時は
   ポリシーで弾かれて空が返る。その時 else 分岐が
   `roomId.slice(0,4).toUpperCase()` を番号欄に入れ、**中身の無い部屋画面を開いていた**。
   **修正内容**: ①`slice(0,4)` を廃止 ②`showCardNotFound()` を新設し、
   専用バナー（🔍「このカードは見つかりませんでした。短縮IDをご確認のうえ、
   もう一度お試しください。」）だけを出して `return` で抜ける
   ③投稿フォーム・お役目終了・削除・共有ボタン・heart-message を全て隠す
   ④catch にも `console.error` を追加
   **文言を「次の方へ渡りました」にしなかった理由**: RLSで弾かれた場合、
   「他人のカード」と「存在しないID」と「破棄済み」が**区別できない**。
   区別できないものを断定してはいけない
   ※実機確認済み（`/card.html?room=zzzzzz` で意図どおりの表示）
   ※**残課題**: この画面から出口が無い（TOPへ戻る／マイページのボタンが出ない）。
     ただし「mypage実機宿題／共通UI・ナビ」に既出の課題であり、この画面固有ではない
15. **RPCのPUBLIC権限を整理する（残6本・大掃除リストへ）**。
   2026-08-04の棚卸しで、18本中7本に `=X/postgres`（PUBLIC）が付いていた。
   うち create_bundle_v2 は監査の剥奪を打ち消していたため同日修正済み。
   残る6本は結果的に実害が無いか内部ガードで守られているが、設計意図とはズレている。
   詳細は「新規確定事項」。**公開直前に大掃除とまとめて実施・前倒し禁止**
16. ✅**完了（2026-08-07）**: print.html の白紙表示が既読機能になっていた件。
   **決着した思想**: 紙は claim の有無を一切語らない。突き合わせは lot_no で行い、
   運営はDBで把握する。「運営が管理のために知る」と「送り主が既読として知る」の
   線引きは、**紙から情報を減らすこと**で解決した
   **前提の訂正**: 旧記述の「claim済みカードは short_id も lot_no も両方DBに無い」は
   **誤り**。reply_and_claim が NULL 化するのは short_id だけで、
   **旧部屋は lot_no を保持している**。よって旧案①（何も出さない）②（位置から計算）は
   どちらも不要になった
   **修正内容（プレビュー2箇所・PDF2箇所の計5箇所）**:
   ・白紙の原因だった門番 `if (r && r.short_id)` を `if (r)` に変更
     （short_id が無いと buildCard / drawPdfCard に入れず、セルが空のまま残っていた）
   ・使用済み判定を `r.is_confirmed || r.closed_at` →
     `r.is_confirmed || r.closed_at || !r.short_id` へ拡張
   ・**黒塗りの小さい文字から short_id を削除し、lot_no だけにした**。
     これが要点。short_id を残すと「確定しただけ＝`4yoftg H0002-001`」と
     「claim済み＝`（空白） H0002-002`」で見分けがつき、**白紙が黒に変わっただけで
     既読の漏れは残ってしまう**。lot_no のみなら両者が完全に同一表示になる
   ※実機確認済み（H0002束で 001・002・004 が同一の黒表示になることを確認）
17. ✅**完了（2026-08-07）**: short_id の紛らわしい文字を除外。
   **印刷前に閉じるべき最優先項目だった**（紙に焼くと二度と直せないため）。
   `generate_short_id` の charset を
   `abcdefghijklmnopqrstuvwxyz0123456789`（36文字）→
   **`abcdefhjkmnpqrtuvwxy0123456789`（30文字）** に変更。
   **除外した6文字＝ g i l o s z**（数字を残して英字を落とす方針。
   そうすると読み替え方向が一意に決まる: i→1 / l→1 / o→0 / g→9 / s→5 / z→2）。
   桁数は6のまま。空間は約22億→**約7.3億通り**で規模的に問題なし。
   `36` の直書きを `n INTEGER := 30` に置き換え、将来の書き換え漏れも防止。
   20連の生成テストで除外文字ゼロを確認済み。
   ※**既存カードには影響しない**。関数冒頭の
     `IF NEW.short_id IS NOT NULL THEN RETURN NEW;`（印刷済みQRの引き継ぎ保護）が
     効いているため
   ※**入力側の自動正規化を入れてはいけない**（既存カードには i・l・o が実在する。
     `nol8om` を `n018om` に変換すると別カードに飛ぶか見つからなくなる）。
     もし将来入力補助を作るなら、**打たれたまま検索 → 空なら正規化して再検索**の
     2段構えにすること
   ⚠**印刷ゲート（2026-08-12）**: 既存 short_id 130件中103件（79%）に旧文字が残る。
     修正後に新規生成された short_id はまだ0件＝30文字セットの実証は本番束生成時。
     **本番束は印刷直前に新規生成する。既存在庫を流用しない**。指示書v1.3へ追記必須

## ドメイン（2026-07-26・取得済み）
- **取得済み: heartcloak.com**（Xserverドメイン・非GMO）
  ・取得日 2026-07-26／利用期限 **2028-07-26**（2年契約）／料金 2年1,721円
  ・サービスコード 22769385／申込ID 105344893／支払デビットカード
  ・**WHOIS代理公開 確定**（XServer Inc.名義。本名・自宅住所とも一切非公開＝実測確認済）
  ・レジストラロック・指定事業者ロックが最初からON（ハイジャック対策）
  ・**自動更新ON済み（2026-07-26確認）**
- 語の意味: cloak＝①預かる（クローク＝一時預かり所・番号札＝short_id・
  タイムカプセル＝時間的に遠い届け先）②纏う（羽織・キャラのクウ）③包む。
  「預ける」と「纏う」は同じ動作の裏表＝クロークに預けるものが外套。
  roomは付けず heartcloak で確定（cloakroomは英BrEで「お手洗い」の婉曲になるため回避）。
  綴りは cloak（時計clockではない）
- 選定経緯（詳細は決定台帳へ）:
  ・短い.com（hcp / hcproject / h-c-p）は全滅または転売屋プレミアム
  ・heartcloakは.com/.jp/.tokyo全て空き・転売屋なし・実在の競合なし
  ・J-PlatPat商標検索（ハートクローク／HEARTCLOAK／ハートクロークス／heartcloaks）
    すべて0件。※簡易検索は対象限定的＝完全な非侵害保証ではない
  ・.jpは更新約4,200円/年・1年契約のみ・更新猶予なし（失効=回復手数料13,280円）・
    氏名の非公開可否が業者次第 → 見送り
  ・.comはWhois代理公開で氏名・住所を確実に隠せる／複数年先払い可
  ・業者はGMO系（ムームー/お名前/Value Domain）を心象理由で回避。
    Xserverは非GMO・大阪の独立系・更新1,721円/年でムームーより安く決着
- **購入後TODO**:
  (1) ✅自動更新ON（2026-07-26済み）。失効=刷ったカード全滅の命綱
  (2) ✅**登録済み（2026-08-12）**: 更新期限リマインダー。
      「プロジェクト」リストに 2028-07-19 9:00 通知（期限2028-07-26の1週間前）。
      メモに サービスコード22769385／申込ID105344893／
      「自動更新ONでも決済カードの有効期限切れで落ちうる＝カードも確認」を記載。
      このHANDOFF記載＋端末の二重で守る方針は継続
      ※**iPhoneのClaudeアプリからのみ登録可能**（PCブラウザにツールが無い）
  (3) ✅**完了（2026-08-12）**: ドメインプロテクションON。**無料**。
      認証用メールアドレスは**個人Gmailのまま**にする（info@heartcloak.com にすると
      メールが止まった時にプロテクションを解除できない循環依存になるため）。
      ネームサーバー変更・移管はロックされる。DNSレコード追加は一覧上のボタンは
      押せる状態だが、**変更実行時に認証が挟まる可能性は未検証**
  (4) .comの更新猶予・復旧手数料のXserver実運用を管理画面/ヘルプで確認
      （一般に.comは期限後30〜45日は復活可・その後救済期間で手数料要、だが業者運用は要確認）
  (5) 認証鍵（AuthCode）は移管時のみ使用。第三者に絶対見せない（「書いたらあかんもの」該当）
- 接続: DNSでWeb→GitHub Pages、メール→さくらに振り分け（下記「サーバー構成とメール」）

## サーバー構成とメール（2026-07-24整理／2026-07-28 事業者決定／2026-08-12 構築完了）
- Webは現構成で足りている。Web用の新規レンタルサーバー契約は不要:
  ・GitHub Pages … 静的サイト配信（Webサーバー）
  ・Supabase … DB・認証・API（バックエンド）
  ※ムームー等の「サーバーとセットでドメイン0円」は不要な抱き合わせのため使わない
- Supabaseは静的サイトの置き場にはならない（ホスティング機能なし）。
  Webの配信はGitHub Pagesのままでよい
- 「裏だから見えない」は誤り。フロントのHTML/JSはPages配信時点で誰でも読め、
  anonキーもブラウザから見える。守っているのは秘匿ではなくRLS＝
  「見えても権限がなければ取れない」設計。7月の監査で固めた層がそれ
- ドメイン取得後の接続先はGitHub Pages。SupabaseのURLは裏方のため変更不要
- ドメイン1つでWebとメールを別々の事業者に振り分け可（DNSのMXレコードで分離）。
  GitHub Pagesはメールを扱えないため、独自ドメインメールは別途用意する
- **メール事業者＝さくら（契約プラン＝さくらのメールボックス）**。稼働中。
  下記3種すべてさくらに寄せている:
  (1) 受信用（問い合わせ受け）… **フォームはWeb3Forms（既存・継続）**。
      静的サイトにHTMLフォームを置き、送信内容をメールに転送する仕組み。
      ページ上にメールアドレスを書かずに窓口を持てるのが利点。
      **転送先を info@heartcloak.com へ変更済み（2026-08-12）**
  (2) 送信用（名乗って返信する）… さくらのメールアカウント info@heartcloak.com。
      ※Web3Formsは受信専用。返信は受信箱側から出るため、転送先がGmailのままだと
        返信した瞬間に個人アドレスが相手に見える＝(1)でメアドを隠した意味が消える。
        転送先をさくらに移したことで行きも帰りもheartcloak.comに揃った
  (3) システム送信（**新規登録のメール確認・パスワード再設定・メアド変更確認**）…
      さくらのSMTPをSupabaseのCustom SMTPに刺す。詳細は「ローンチ前タスク」。
      ※Web3Formsとは無関係の別系統。混同しないこと
- ⚠**「マジックリンク」ではない（2026-08-12・重要な訂正）**。auth.htmlの実装は
  `signInWithPassword`＝**メールアドレス＋パスワード方式**。
  **ログイン時にメールは飛ばない**。メールが飛ぶのは3箇所だけ:
  `signUp`（778行付近・新規登録）／`resetPasswordForEmail`（826行付近）／
  メアド変更時。Supabaseのテンプレート「Magic link or OTP」は**未使用**。
  ※HANDOFF内に「マジックリンク」表記が残っていたため長く誤解が続いた。
    以後この語をこのプロジェクトで使わない
- **DNSゾーンは1個**。同じゾーンにWeb（GitHub Pages）とメール（さくらのMX＋
  SPF/DKIM/DMARC）が同居している。
  **ネームサーバーはXserverのまま＝A案で決着（2026-08-08）**。
  さくら側は「他社で取得したドメインを移管せずに使う」で追加し、
  さくらが案内する ns1/ns2.dns.ne.jp への変更には**従っていない**。
  さくらのコンパネ内「DNSレコード設定」は**このプロジェクトでは使わない**
  （書いても誰も見ない。書き込み先はXserver側が正）
- 運営者情報ページに個人のGmailを晒さないため、(1)(2)は公開前に用意する→**用意済み**

## ローンチ前タスク（2026-07-24起票／2026-07-28 メール方針確定／2026-08-12 メール構築完了）
### メール(3) システム送信（✅完了 2026-08-08〜08-12・Resendは予備）
- **契約**: さくらのメールボックス／12ヶ月一括 1,320円（税込・初期費用0）。
  次回以降も同額。**12〜36ヶ月一括のみで月払い不可**。
  会員ID iit70499／サービスコード 113801779213。
  ※当初「スタンダード（月660円〜）」想定だったが、Webは GitHub Pages で足りており
    Web機能が完全に余るため下位のメールボックスへ変更。年額で約5分の1
- **確定値（Supabase Custom SMTP に投入済み）**:
  Host `heartcloak.sakura.ne.jp`（**初期ドメイン**。SPFに書いた
  `www3110.sakura.ne.jp` ではない）／Port 587／
  Username `noreply@heartcloak.com`（**@以降まで含むアドレス全体**）／
  Sender email `noreply@heartcloak.com`／
  Sender name `こころづて / HeartCloak Project`／
  Minimum interval per user 60秒。
  サーバーのホスト名 www3110.sakura.ne.jp／IPv4 49.212.207.150
- **Rate Limits**: Custom SMTP有効化で初期値30通/時。100通/時へ引き上げ済み。
  さくら側は250通/15分＝1000通/時のためSupabase側が先に効く。
  上げすぎない理由＝noreply@のパスワードが漏れた場合、その上限まで送られる
- **DNS4件（Xserver側に登録・外部検証済み）**:
  MX `heartcloak.com` → `www3110.sakura.ne.jp`（優先度10）／
  TXT `heartcloak.com` → `v=spf1 a:www3110.sakura.ne.jp mx ~all`／
  TXT `rs20260808._domainkey` → `v=DKIM1; k=rsa; p=...`（410文字・2048bit）／
  TXT `_dmarc` → `v=DMARC1; p=none; rua=mailto:info@heartcloak.com`。
  MXtoolboxでMX/SPF/DKIMの3種とも合格を確認（SPFは12項目すべて緑）。
  応答元が ns1/ns2.xdomain.ne.jp ＝A案が機能している証拠。
  既存のAレコード4本・www CNAMEは無傷
- **メールアドレス**: info@（問い合わせ受け）／noreply@（システム送信）。
  どちらもウイルスチェック有効・**迷惑メールフィルタは「利用しない」**。
  理由＝この用途は取りこぼしが致命的で、受信箱に届く前の振り分けは事故になる。
  高精度フィルタはメール原文が第三者(Hornetsecurity)に渡る点も判断材料。
  ※**パスワードはinfo@とnoreply@で別のものにする**。noreply@はSupabase管理画面に
    保存されるため、使い回すとSupabase侵害時にinfo@の受信箱まで開かれる
- **採用理由**: 契約1本で済む／SPF・DKIM・DMARCがさくら側で完結／送信数は桁で余裕。
  かつCustom SMTPの設定欄は host・port・user・pass・送信元 の5項目のみ＝
  **後からResend等へ差し替えてもコード変更ゼロ**。先に凝らない（エスカレーション原則）。
- **リスクと撤退条件（継続監視）**: 共有プランは送信元IPが他ユーザーと共有のため、
  他人のスパム判定に巻き添えでGmail等に落ちる可能性がある（さくら自身も明記。
  専用送信元が要る場合はビジネスメールプランに固定IPあり）。
  Gmailに落ちる兆候が出たら(3)だけResendへ移す。
  Resend無料枠は月3,000通・日100通・1ドメイン。
  **A案（NSはXserver）のためMXを1行書き換えるだけで移行できる**
- **踏んだ罠（再訪時の注意）**:
  1. 初期ドメイン名は**申込完了後に変更できない**。`heartcloak` で確定。
     SPFに載る＝公開情報なので本名等は入れられない（隠す意味もない）
  2. 契約直後は**お試し期間（2週間）**に入り送信数が別枠で絞られる。
     クレジット払いでも自動決済は期間終了後。**会員メニューの「本登録」を
     明示的に押す**と即時決済され制限が解除される。放置すると原因不明の不達になる
  3. 電話番号認証の解除は**サーバーコントロールパネル**から。会員メニューではない。
     ログインは「初期ドメイン／パスワード」で入る（メールアドレスで入ると
     メール設定しか出ない）。認証後に「再読み込み」→「メール送信制限解除」まで
     押さないと解除されない
  4. XserverのTXT入力欄は**先頭に空白が混じると「内容の記述が正しくありません」で弾かれる**。
     DKIMが通らない時はまずこれを疑う（255文字制限ではなかった）
  5. さくらの「DKIM/ARC設定」画面はPEM形式（改行入り）で公開鍵を出す。
     DNSに書くのは `v=DKIM1; k=rsa; p=<BEGIN/END行と改行を除いた1本の文字列>`。
     **さくらは他社DNS向けに整形した値を出してくれない**（B案前提の作り）
- **未検証・保留**:
  ・DMARCは `p=none`（レポートのみ）。運用が安定したら quarantine へ上げる判断
  ・GoogleからDMARCレポートが**毎日 info@ に届く**（ZIP圧縮XML・文字化けして見える）。
    溜まって邪魔になったら rua= を専用アドレスに逃がす
  ・さくらの「振り分け転送」は条件を3パターン試したが**動作を確認できず削除**
    （題名の空欄／送信先To／差出人From）。原因未特定。
    併配信は下記のとおりWeb3Forms側で実現しているため深追いしない
  ・⚠**新SMTPで signUp が一度も走っていない**（新規登録は2026-05以降ゼロ）。
    既存4件中3件は confirmation_sent_at が null ＝当時メール確認OFFの疑い。
    **捨てアドで登録を1回通す（β前必須）**

### メール(1)(2) 問い合わせ窓口（Web3Forms継続・✅完了 2026-08-12）
- **転送先を info@heartcloak.com へ変更済み**。これで返信時のアドレス露出も解消。
  **当面は個人Gmailと併配信**（Recipient Emails に2件指定）。
  慣れたらGmailのチェックを外して1本化する＝設定1箇所で切れる。
  ※Linked Emails は info@ のみ登録（1/2使用）。個人Gmailはアカウントの
    primary address 扱いで、Linked Emailsには追加できない（エラーになるが正常）
- **フォーム設定の修正**: Form Name を「お互い様プロジェクト」→
  「こころづて / HeartCloak Project」。Website URL を旧Netlify
  （keen-marshmallow-2a5ebc.netlify.app）→ heartcloak.com
- ⚠**通知メールの差出人はWeb3Forms（noreply@web3forms.com系）になる**。
  独自ドメインから出すのはPro機能のため無料枠では変えられない。
  返信先は Reply-To に問い合わせ者のアドレスが入るため返信自体は正しく届く。
  件名は contact.html の hidden input で `【ご意見】こころづて` に固定
- **hCaptcha有効化済み（2026-08-12）**。無料枠で選べるのはhCaptchaのみ
  （Cloudflare Turnstile・Google reCAPTCHAはPro）。
  **ダッシュボードでONにするだけでは動かず、contact.htmlへの追記が必須**:
  ①フォーム内に `<div class="h-captcha" data-captcha="true"></div>`
  ②`<script src="https://web3forms.com/client/script.js" async defer></script>`
  ③送信処理の冒頭で `textarea[name=h-captcha-response]` の値をチェックして未入力なら中断
  ④送信失敗時に `hcaptcha.reset()`（トークンは1回で無効になり再送信できないため）
  実装済み・パズル表示と送信成功を実機確認。
  送信元ドメインを制限するRestrict to Domainsは有料プラン限定のため、
  無料枠のままだとキーを拾った第三者にフォーム外から連投され、月枠を食い潰されうる
- 無料枠は月250件・送信履歴は30日で消える。
  **問い合わせの保存場所は実質メールボックス側**＝さくらに寄せる理由のひとつ
- Web3FormsのAccess Keyは**シークレットではない**（公式明言・HTML直書き前提の公開値。
  メールアドレスの別名として機能する）。「書いたらあかんもの」には該当しない

### 検索まわり（SEO・2026-08-02起票）
- 現状: index.html に title／meta description／og:title・description・url を入れただけ。
  それ以上の対策は未実施
- **Googleは旧名称で記憶している**。2026-08-02時点の検索結果は
  「サポートメンバー募集 — お互い様プロジェクト」でsupport.htmlが代表ページ扱い。
  中身は差し替え済みだが、再クロールされるまで表示は古いまま（数日〜数週間）
- 未着手の候補（優先順）:
  1. **Google Search Console 登録**。名称変更の反映を早めたいならこれが一番効く
     （インデックス登録のリクエストが出せる）。他は急がなくてよい
  2. sitemap.xml / robots.txt の設置（各10行程度。どのページを見せたいかを伝える手段が今ゼロ）
  3. index以外のページに description・OGP を入れる（SNSでURLを貼った時の見た目も同時に改善）
- 判断軸: β版で集客する段階でなければ2・3は急がない。
  「お互い様プロジェクトで覚えられている状態」をどれだけ早く消したいかで決める

### その他
- リポジトリ公開範囲の判断（既出・未了）: otagaisama/otagaisamaはpublic。
  ①現状維持 ②docs/だけ別privateリポジトリ ③GitHub Pro月$4で全体private。
  ②③にするとチャット版Claudeがraw URLから読めなくなる（Claude Codeは影響なし）。
  ※注意1: ファイルを消してもコミット履歴から読めるため「消す＝隠す」にはならない。
    履歴に残って困るものは最初から置かない
  ※注意2: 本番HTML/JSはprivate化しても配信時点で誰でも読める。
    隠す対象はdocs/等の設計メモであって本番コードではない
- URLの安全性・信頼設計（別チャットでテーマ立て）:
  ・運営者情報ページ（屋号・問い合わせ・何をやっているか）
  ・開いた瞬間の第一印象／HTTPS・証明書（Pagesは自動）
  ・Googleセーフブラウジング等の外部チェック（弁明用・攻めには効かない）
  ・QRすり替え対策（他人が偽QRシールを上から貼る手口。カードの意匠・材質・
    裏面の作りで対策する＝印刷デザインと同時に詰める。優先度高）

## 印刷カード裏面（2026-07-24確定・指示書へ追記済／2026-07-26 内訳追記／2026-08-02 QR系統分け・ECC決着／2026-08-07 文字セット確定）
- 面の配分: 裏面91×55mm。QRは1/4〜1/2枠。残りに作成者名・説明書き
- 裏面要素の内訳: QRブロック 左約33mm（QR実効27.5mm＋端マージン）／
  作成者名・説明書き 右約58mm
- QR実寸22mm（下限20・上限25）。1マス0.667mm＝安全圏（危険域は0.4mm以下）。
  ※この22mmはURL短縮後の33モジュールを前提とした数字
- **QRは系統分けで決着（2026-08-02）**。印刷屋＝22mm、**家庭用＝16mm**。
  22mmは印刷屋の両面レイアウト（裏面QRブロック33mm確保）から来た数字であり、
  家庭用に機械的に持ち込む必然性がないため。詳細は docs/家庭用カード仕様_2026-07-29.md
- 実測（2026-07-28・家庭用プリンタ＋型落ちiPhone）: **10mmまで読取成功**
  （1マス0.303mm）。危険域0.4mmを大きく下回っても読める＝基準値は保守的。
  デザイン都合で縮める場合16〜19mmまでは裏付けあり。
  **2026-08-04追記: 16mm実運用でも受け手側の読取成功を確認**（#256を知人に手渡し）。
  残：ケント180kg入手後に本番用紙で再検証
- クワイエットゾーン: QR周囲4モジュール分の白地（22mm時=約2.7mm）。
  この領域に文字・罫線・背景色を置かない。実効27.5mm四方を確保
- 配置: トリム端から5mm以上内側（4丁付け断裁の±1〜2mmズレ対策）。
  「中央」である必要はない
- QRは白地に黒・単色。反転や背景写真への重ねは不可（意匠を捨てる場所）
- URLは取得済みドメインで確定: https://heartcloak.com/receive.html?room=<short_id> で焼く。
  **入口パスは直焼きで決着**（/r?i= リダイレクタ案は不採用）。
  print.html 339行 RECEIVE_BASE 差し替え済み（2026-07-28）。
  URLは64→47文字に短縮され、QRは37×37→**33×33モジュール**になった
- 短縮URLサービス（bit.ly等）は使わない。自前ドメイン直で焼く。
  短縮サービスはサービス終了・買収で消えるとカード全滅するため（指示書にも明記）
- **ECC（誤り訂正レベル）決着（2026-08-02）**: print.htmlの実装はMだった。
  家庭用は**Qに変更**（欠損耐性15%→25%。v4 33×33→v5 37×37に繰り上がるが、
  16mmでも1マス0.432mmで実測下限0.303mmに対し43%の余裕）。
  印刷屋側も指示書にQで明記すること（実装チャットに判断させない）
- **フォントの制約（2026-08-02発覚・重要）**: fonts_subset.js はカード文言に使う
  文字だけのサブセット。**文言を変えたら必ず再生成**しないと豆腐になる
  （実測で日本語26字が不足していた）。さらに **`β` は Shippori Mincho に収録がない**
  （ギリシャ文字）ため、ラテン用の別フォント併用が要る。
  **「こころづて」の可否を実測（2026-08-02）**: FONT_SMR（明朝レギュラー）に
  「ろ」「づ」が無く、FONT_SMB（明朝ボールド）には「こ」「づ」「て」「ろ」が無い。
  カード面を新名称にするには fonts_subset.js の再生成が必須
  ※**2026-08-07の short_id 文字セット変更により、サブセットに要る英数字が減った**
    （g i l o s z の6文字が short_id に出なくなる）。ただし lot_no や他の文言でも
    使うため、**サブセットから外してよいとは限らない**。再生成時に実文字を洗うこと
- **print.html のシート表示仕様（2026-08-03実機確認／2026-08-07に修正完了）**:
  ・**確定済み（is_confirmed=true）のカードは黒地に「使用済み」**と表示される。
    再印刷時に使用済みを配らないための表示。
    closed_at は見ていない（#256は closed_at なしでも黒塗り。
    #259 は closed_at 有りだが表示は #256 と同じ＝**閉じたかどうかは表示に出ていない**）
  ・**「使用済み」表示はバトンが繋がったことを意味しない**。判定は is_confirmed のみ。
    確定＝メッセージを書き終えた段階であり、受け手の返信・claim とは無関係。
    黒塗りでも closed_at が null ならお役目終了はまだ打てる
  ・**✅ 白紙問題は2026-08-07に解消**（次の一手16）。
    以前は short_id が NULL のカードが何も描かれず白紙になり、それが既読通知として
    働いていた。現在は判定を `is_confirmed || closed_at || !short_id` に広げ、
    **claim済みカードも同じ黒塗りで描かれる**。
    **黒塗りに出す小さい文字は lot_no のみ**（short_id は出さない。
    出すと有無で claim の有無が判別できてしまうため）
- 紙に刷る中身の注意:
  ・作成者欄に本名を刷らない（whoisで隠す意味が消える）。屋号かプロジェクト名
  ・可変情報（連絡先・説明文言）は極力Web側。紙は最小限
  ・法定表記（特商法・電気通信事業法）は紙に刷らない＝全部Web側
  ・short_idの人間可読印字は保険として有効。
    **✅ 2026-08-07に生成側で紛らわしい文字を除外済み**（g i l o s z を除いた30文字）。
    **ただし既存カードには i・l・o が実在する**（`nol8om` `a79im9` `4yoftg` 等）ため、
    **入力側で紛らわしい文字を自動変換してはいけない**（別カードに飛ぶか見つからなくなる）。
    入力補助を作るなら「打たれたまま検索 → 空なら正規化して再検索」の2段構え
  ・HPアドレスはトップのみ。ID込みの手打ちは非現実的
  ・「スキャンしてください」相当の一言は必須。
    **英語1行は家庭用では入れない（2026-08-02）**。印刷屋側は
    インバウンド配布インフラ構想が動く場合に再判断
- **表記の確定（2026-08-02）**: `HeartCloak Project`（半角スペース区切り）。
  ハイフン結合・詰め書きは不採用。綴りは cloak（clockではない）
- **空欄を作らない原則（2026-08-02確定・再提起しない）**:
  手書き用の空欄を紙に刷らない。運営が管理できない印字面を作ると、判子等で
  意図しないものが乗り、半永久で回るカードに延々巡回する。
  詳細は docs/個人名入りカード構想_2026-08-02.md
- short_id桁数: 6桁で40文字＝v3。8桁まではv3のまま、9桁でv4（それでも安全圏）。
  設計はv4（62文字）想定でmmを決める＝将来桁が伸びてもレイアウト作り直し不要。
  バッチ内は桁固定（6桁と7桁が混ざるとQRの細かさが変わり束内で見た目がバラつく）。
  **2026-08-07に文字セットを30種へ縮小したため、6桁で約7.3億通り**
  （旧36種＝約22億通り）。2,000枚では衝突ほぼ無し
  ※**旧世代に5桁の short_id が実在する**（`0ei51` `hui47`＝#68・#72）。
    初期採番の名残。実害は観測されていないが、桁固定の前提として認識しておく

## mypage整理ボタン（2026-07-22実装・要点）
▼この回の詳細な経緯・判断はこのチャット参照:
  チャット名「IDの出現について」
  URL:https://claude.ai/chat/cb22ced2-bed4-4f34-b9f7-b7e2d957b3e8
- 背景: 7/14に「お役目終了はcard.html側に集約」した結果、mypage一覧の整理ボタンが
  消えていた。H0001束（自家印刷10枚）が手持ちタブに並んで初めて「整理手段ゼロ」が露呈。
- 確定した思想: 役目終了（名刺破棄）の対象は「所有権あり・未確定」のカードのみ。
  確定（is_confirmed=true）＝送った＝所有権が手を離れる、ので破棄対象外。
  URL部屋の削除も同条件（所有権あり・未確定・URL型）。名刺破棄と削除は対象条件が対称、
  違いは現物の有無だけ（QR=破棄／URL=削除）。両者ともRPC側でも所有権・状態・種別を検証。
- 実装: 手持ちタブ（renderHoldingPage）を作り直し。由来バッジ（じぶん印刷/QR/URL）＋
  状態バッジ＋整理ボタンを追加。QR名刺＝「名刺破棄」（discard_card）、
  URL部屋＝「削除」（delete_room）。手持ち専用関数 discardCardHolding/deleteRoomHolding
  を新設（holdingRoomsAll更新→再描画）。送った・完結タブの旧お役目終了ボタンは撤去。
- 表現: 「名刺破棄」に統一（「削除」だと心のカード削除と誤解するため）。確認文言に
  「心のカードそのものは削除されません」を明記。
- 同時実装のUI調整5件（2026-07-22・差し替え反映確認済み）:
  ①ロット番号の視認性UP（9px金→13px濃色・金枠バッジ・名刺照合用）
  ②上バーの管理/ログアウトの色（白→濃インク・薄背景で見えなかった）
  ③タブ内ソート（手持ち・送った：番号昇降/日付新旧のプルダウン。もらったは見送り）
  ④「ペンネームを変更する」→「基本情報変更」に文言変更
  ⑤β参加日数の表示を削除
- DB変更なし（既存RPC流用）。旧sent用関数 retireCardList/deleteRoom は死蔵で残置（将来流用可）。
- 反映後の実機確認: ①手持ちにH0001束が「じぶん印刷+未使用+名刺破棄」で並ぶか
  ②名刺破棄が1枚で動くか ③URL部屋に削除ボタンが出るか ④ロット枠が読みやすいか

## mypage実機宿題（2026-07-22・未着手）
※1個ずつ別チャットでテーマ立てて対応。詳細な文脈は上記「mypage整理ボタン」の
  チャットリンク参照。片付いたらdocsへ逃がす（運用ルール⑨）。

【機能追加（設計要・別チャット）】
- もらったタブにソート＋削除ボタン（今回見送り。ID配列で追加取得要・
  「もらった心を消す」意味の設計注意＝記録から消す vs 実体を消す を先に詰める）
- card.html（部屋の中）に削除・お役目終了ボタン＋#番号表示。
  ※**2026-08-02に決着**。card.html にURL部屋用の削除ボタンを追加（QR名刺は
    「お役目を終える」、URL部屋は「削除」で対称になった）。
    お役目終了が確定済み部屋にも出るのは**正しい**（確定後の汚れ・破損・紛失に必要）。
    未確定でも出るのも正しい（在庫整理に必要）。表示条件は現状維持で確定
- 手持ちタブの種別セクション分け（QR/URL/じぶん印刷）
- 返信ありの通知
- QRの安全な読み込み
- 部屋生成の導線短縮（TOP経由が「作成→生成→QR表示→部屋」と長い。
  マイページ=心を届ける／TOP=カード作成受け取りから、の2経路を整理）

【ログ機能】
- **2026-08-12に docs/運営ダッシュボード_2026-08-12.md へ全面移設**。
  列構成・IP未取得の判断・JST表記・記録の仕組み・保存クエリ2本・読み方の鉄則・
  未ログイン棚卸し・取得種類の拡張候補は、すべてそちらが正。ここには書き戻さない。

【デザイン段階】
- QR表示→保存の導線
- 協賛会社表示デザイン
- QR大きさ調整 → **2026-08-02に決着**。家庭用は16mm＋クワイエットゾーン1.94mm・
  ECC Q（37×37・1マス0.432mm）。印刷屋は22mmのまま。系統分けで確定。
  print.html への反映は両面化改修と同時（次の一手8）
- クウの羽織デザイン（別チャット・2026-07-24起票）。cloakの二義（預かる/纏う）を
  絵にする。羽織＝一時的に預かる物・脱げる／御朱印＝集めて残る物、の層を混ぜない。
  新選組のだんだら模様は史実側から取れば権利問題なし（特定作品のキャラデザに寄らない）

【共通UI・ナビ】
- TOPへ戻る／ログアウトが前ページ（card.html等）に無い
- 右上マイページボタンが見えないページがある
- **カード見つからない画面にも出口が無い**（2026-08-07追記）。
  次の一手14の修正で追加した専用画面も、この課題の対象に含まれる

【バグ・要再現】
- 「カード見つかりません」→戻るボタンでバグる時がある（再現条件を特定してから）
- ✅**card.html の catch が例外を握り潰していた件は2026-08-07に修正済み**。
  1181行に `console.error('部屋作成エラー:', error);`、
  1191行に `console.error('部屋生成エラー:', e);` を追加。
  2026-07-29の障害切り分けで、Console空・Network0件・画面には「通信エラー」だけ、
  という状態になり原因特定に時間を要した問題は解消。
  さらに次の一手14の修正で、部屋読み込みの catch にも
  `console.error('カード読み込みエラー:', e);` を追加した
  ✅**receive.html 361行の `.then(null, () => {})` も2026-08-15に修正済み**
    （console.error 追加）。同時に card.html 1269行のログ呼び出しを削除し、
    二重カウントを解消した（次の一手12）
- ✅**「G666」表示は2026-08-07に修正済み**（次の一手14に詳細）。
  ・**該当カードは g666me**（旧 #257 = id277 = lot_no H0002-002 →
    claim後の新 #267 = id287）。「G666」は `roomId.slice(0,4).toUpperCase()` による
    表示で、short_id の頭4文字がそのまま出ていただけ
  ・**真因は RLS**（旧記述の「claim前後の隙間」は誤り）。rooms の SELECT ポリシーは
    3本とも自分の部屋限定で、匿名に開かれたものが1本も無い。
    claim で g666me が新部屋へ移ると、元の持ち主が同じURLを開いても弾かれて空になる
  ・**card.html だけが rooms を直に select している**（receive.html は
    `get_card` RPC 経由で SECURITY DEFINER のためRLSを跨ぐ）。
    ただし card.html は本来「自分の部屋を編集する画面」なので、
    自分の部屋しか見えないこと自体は正しい。壊れていたのは見えなかった時の出口だけ
- **card.html 画像アップロード不具合（2026-08-02発見・同日修正・実機で解決確認済み）**。
  症状＝画像を選んで確定すると、成功トーストが出るのに画像だけ消える。
  原因は2つ重なっている。
  ①**確定時のStorageパスに short_id を使っていた**
  （`${currentRoomId}/…` → 正しくは `${currentRoomUuid}/…`＝rooms.room_idのUUID）
  ②アップロード失敗を `if (!uploadError)` で握り潰し、imageUrl=null のまま
  insert が通っていた。画面に見えていたのはローカルのプレビュー
  ※**一時保存を挟むと助かる**（保存で正しいパスに上がる）。
    「以前は動いていた」の説明がつく
  ※`currentRoomId` はURLパラメータ生値（QR型ならshort_id）、
    `currentRoomUuid` は rooms.room_id。混同しないこと
  ※**QR名刺でのみ発生するバグだった**。URL部屋は `?room=` にUUIDが入るため
    修正前から動いていた。検証にはQR名刺を使う必要がある
  ※関連: 自動保存は**文字入力でしか走らない**（画像選択では予約されない）。
    打つ順番で結果が変わっていたのが「以前は動いていた」の正体
- **既に確定済みのカードの画像は復旧できない**。Storageに実体が無いため
- **確定後の修正機能は追加しない方針**（2026-08-02判断）。
  一枚一往復・確定後修正不可は設計の骨格。代わりに**確定の手前を厚くする**
- **手持ちタブのH0001束10枚が全て状態バッジ「書きかけ」**（「未使用」ではない）。
  ※バッジは `isBlank ? '未使用' : '書きかけ'` の二択で、下書きの有無を見ていない。
    is_blank が何を見ているかはDB側（get_card）のため未確認
- **受け手に送り主が「匿名」と出る（2026-08-02発見・修正済み/実機未確認）**。
  ペンネームは **rooms.pen_name** に持つ。QR名刺の部屋は print.html 側で
  作られるため空のままだった。**次に確定する新しいカードから埋まる。既存は空のまま**
- **mypage の並び順プルダウンが、戻るボタンで表示だけ初期値に戻る**（実害は表示のみ）
- **手持ちタブに反映されるまでのラグが大きい**（バトンで所有権が移った後）
- **マイページのリクエストが多い**（1画面で98件・実測1.39秒）。
  2026-08-02に「送った」タブ分を2本→1本へ削減済み。現状は実害なし
- **GoTrueClient 複数インスタンス警告**（Console・実害なし）
- **profiles テーブルに行が無い**（2026-08-02観測）。
  ※2026-08-07にmypageコードを確認。906行で `profiles.name` を読み、
    無ければ user_metadata へフォールバックする作りのため**動作に支障なし**。
  ※**2026-08-12に位置づけを整理し「新規確定事項」へ移した**。
    実カラム5つ・SELECTポリシー1本のみ・フロントに書き手なし（トリガー生成）。
    残る判断は `profiles.email` 列の要否だけ＝大掃除リスト行き

## 新規確定事項（2026-07-18〜20監査／2026-08-03〜07追記／2026-08-12追記）
- admin系操作はoperator限定ポリシーで防御済み（クライアント判定は飾りでよい）
- XSS: ユーザー入力の出力箇所 全16箇所エスケープ済み・シロ
- 関数: SECURITY DEFINER 17本すべてsearch_path固定済み
- RPC権限層（7/20第2次）: 全18本のEXECUTE×内部ガード判定完了・穴は同日修正済み
- **rooms の SELECT ポリシーは3本・すべて自分の部屋限定（2026-08-07実測）**:
  `holder can read owned rooms`（auth.uid() = owner_user_id）／
  `owner can read own rooms`（auth.uid() = user_id）／
  `operator can select all rooms`（is_operator()）。
  **匿名に開かれたSELECTポリシーは存在しない**。
  したがって rooms を直に select しているページ（card.html）は、
  他人の部屋・未ログインでは必ず空を返す。G666の真因はこれ（次の一手14）
- **RPC権限の再棚卸し（2026-08-04・全18本のproaclを実測）**:
  ・**create_bundle_v2 のPUBLIC残存を発見→同日 revoke 実施**。
    7/20にanonを剥がしたが `=X/postgres`（PUBLIC）が残っており、
    **PUBLICはanonを含むため剥奪が実質的に打ち消されていた**。
    proacl再確認で postgres / authenticated / service_role の3行のみになったことを確認
  ・**anon と PUBLIC の違い（用語メモ）**: anon は「ログインしていない利用者」という
    役職名。PUBLIC は「全員」という総称で、**anon も authenticated も含む**。
    したがって anon だけ revoke しても PUBLIC が残っていれば anon は通ってしまう
  ・**proacl が null の関数は無し**＝7/20の revoke→grant 儀式は全関数で通っている
  ・**PUBLIC が残る関数（残6本・大掃除リスト行き）**:
    claim_blank_card ／ delete_room ／ generate_short_id ／ handle_new_user ／
    log_card_access ／ reply_and_claim ／ reset_blank_and_claim
    （うち claim_blank_card・log_card_access・reply_and_claim は anon 明示付与済み＝
     冗長なだけ。generate_short_id・handle_new_user は内部専用のはずで意図とズレる。
     delete_room・reset_blank_and_claim は関数内の `auth.uid()` ガードで守られている）
  ・**教訓**: 権限を絞ったつもりの関数は、**anon だけでなく PUBLIC も確認する**
- **`rooms.lot_no` 列は既に存在する**（2026-07-29実測）。
  残るのは bundles 側のlot_no付与とmypage表示の実装であり、列追加ではない
- **reply_and_claim の中身（2026-08-03にコードで確定／2026-08-07に修正）**:
  ・トリガーは**受け手の返信**。確定（is_confirmed）ではない
  ・世代交代が走るのは **`room_type = 'qr'` かつ short_id が非NULL の時だけ**。
    **URL部屋は世代交代しない**（新部屋も新global_noも生まれない）
  ・処理は ①room_posts に reply を刻む ②旧部屋の short_id を NULL 化し
    owner_user_id を返信者へ ③新部屋を INSERT ④room_transfers に 'reply_claim' を記録
  ・✅**新部屋の INSERT に lot_no を追加済み（2026-08-07）**。次の一手11
  ・**同じ欠落が claim_room・reset_blank_and_claim にもあった**。3本とも修正済み
  ・**旧部屋の lot_no は NULL 化されない**（消えるのは short_id だけ）。
    したがって claim 後は旧部屋・新部屋の両方が同じ lot_no を持つ。
    紙が1枚なのだから両世代が同じ紙を指すのは正しい
  ・旧部屋に残るのは `user_id`（元の作成者）のみ。owner_user_id は書き換わる
  ・ガード: 未ログイン／card_text空または25字超／部屋なし／closed_at有り／
    未確定／自分のカード／返信済み、はいずれも早期リターン
  ・**closed_at 有りで弾かれる＝閉じたカードは返信も取り込みもできない**。
    返信とclaimは同一関数内で一続きのため、返信が弾かれればclaimも起きない
  ・**お役目終了（closed_at）では short_id は動かない**。short_id が移動するのは
    claim の時だけ。#259（nol8om）が閉じても short_id を保持しているのは正常
- **`generate_short_id` は BEFORE INSERT トリガー関数（2026-08-07実読）**:
  ・冒頭に `IF NEW.short_id IS NOT NULL THEN RETURN NEW;` があり、
    **明示指定があれば素通しする**（印刷済みQRの引き継ぎ保護）。
    このおかげで生成ロジックを変えても既存カードには一切影響しない
  ・重複チェックは `EXIT WHEN NOT EXISTS (SELECT 1 FROM rooms WHERE short_id = result)`
    のループで担保されている
  ・✅**2026-08-07に charset を30文字へ縮小**（次の一手17）
- **mypage の集計ロジック（2026-08-07・コードで確定）**:
  ・**「届けた」= rooms のうち `user_id が自分` かつ `is_confirmed = true` の件数**。
    自分が作って確定した部屋だけを数える
  ・**「受けとった」= room_posts のうち `user_id が自分` かつ `post_type = 'reply'`**
    の件数（room_id で重複除去）
  ・**返信は「届けた」には入らない**。返信は rooms を作らないため
  ・**URL型でも「もらった」に入る**。集計は room_type を一切見ていない。
    claim が起きないのは所有権の話で、集計とは別系統
  ・**ポイント式が生きている**（949行 `sentCount * 1 + receivedCount * 2`）。
    表示は 785行の `style="display:none"` で隠しているだけ。**休眠コードであり
    デッドコードではない。消さない**
  ・**重み付け1:2の根拠＝「感謝をするより、されるほうが難しい」**。
    感謝を書くのは自分の意思でできるが、感謝されるのは相手の中で何かが
    動かないと起きない。**評価の優劣ではなく、発生しにくさに対する燃料の差**。
    起こりにくいことが起きたら燃料が多い＝筋トレのロジックと同型。
    → **決定台帳の第3章候補**。コードにも意図を1行コメントで残す
- **`log_card_access` はDB側シロ（2026-08-04）**: EXECUTE権限は anon にあり、
  関数本体も insert 1行のみで未ログインを弾く条件は無い
- 教訓: 「auth.uid() is null＝SQL Editor」は誤り（anonキーREST直叩きでもnull）。
  新RPC作成時は revoke→必要ロールにgrantを儀式化
- card_access_log: 稼働中（列名はaccessed_at）。デバッグタスクは廃止
- claim_bundle現行版は正常（claimed_at記入あり）
- draft+message併存の原因はDELETEポリシー欠落→追加済みで再発防止。
  既存分（F点呼: 22件・2026-07-19時点）は大掃除リストへ集約
- lot_no採番: {ロット番号}-{束内position3桁} 形式（印刷屋=「0001-047」、
  家庭用=頭にH「H0001-047」で予約・区別）
- bundles.lot_id列とadmin手動割当UIは既存。lot_no表示は実装済み（mypageに出る）
- ✅**auth.html の profiles.upsert を削除（2026-08-12）**。
  doRegister() 内に残っていた11行（791〜801行）と直後の空行を削除。885行→873行。
  md5 `b5602e0f758e74f7f4b70be8ef8c6631` → **`fd5463883f810e98988885a32b92fb84`**。
  **auth.html から `profiles` / `username` の文字列は完全に消滅**
  ・**このコードは二重に失敗していた**（2026-08-12にDB実測で確定）:
    ①送っていた `username` 列が**そもそも存在しない**
    ②profiles には **INSERT/UPDATE のポリシーが1本も無い**ため、
      仮に列があっても RLS で必ず弾かれる
    ③さらにメール確認ONのため実行時点で `data.session` が null＝**未ログイン(anon)**
  ・**`try/catch` は何も捕まえていなかった**。supabase-js v2 の `.upsert()` は
    HTTPエラーで reject せず `{ data, error }` を返すため、catch に入るのは
    ネットワーク断のときだけ。**戻り値を見ていないことで黙殺されていた**のが実態。
    コメント「テーブルなくてもOK」は結果は合っているが機構の説明が誤り
  ・**HANDOFF の「profiles.username は存在しない」は正しかった**＝訂正不要
- **profiles の実カラムは5つ（2026-08-12実測）**: `id`(uuid) / `name`(text) /
  `created_at`(timestamptz) / `email`(text) / `role`(text)。
  ・読み手は mypage.html 908行（`name, role`）と admin.html 198行（`role`）の2箇所のみ。
    **書き手はフロントに存在しない**（行の生成は `handle_new_user` トリガー）
  ・**RLSポリシーは1本だけ**＝`profiles: own read`（SELECT / authenticated /
    `id = auth.uid()`）。INSERT・UPDATE・DELETE のポリシーは**存在しない**
  ・**`profiles.email` は完全に孤立した**（上記削除で唯一の書き手が消え、読み手は元から無い）。
    auth.users 側に email がある以上は重複保持＝**列の要否を大掃除リストで判断**
  ・**INSERT/UPDATEポリシー不在が意図的かの記録が無い**。トリガー(SECURITY DEFINER)経由
    のみで行が生まれる設計としては正しいが、根拠が残っていない＝**決定台帳の候補**
- 軽微な堅牢化 残1件（非公開管理・詳細は監査チャット）→大掃除リストと同時に実施
- 掃除系タスクはすべてdocs/大掃除リストに集約済み。実行は本番公開直前・前倒し禁止
- Supabase側仕様変更（2026-07）: log_connections新デフォルトoffへ自動移行・対応不要
- .nojekyll追加（2026-07-20）: Pages のJekyll処理を無効化。削除禁止

## DB観測メモ（2026-07-22・実測／2026-08-03〜07追記）
7/11宿題の退行チェックをDB直クエリで実施。退行なし。以下は観測記録。
- 空部屋（room_posts 0件）: 最初期テスト id1〜3（owner NULL）＋#16=id26 ほか、
  7月の束claim実験の残骸群。全て大掃除リスト対象・現時点は放置で正。
- 白紙self_claim 21枚 = 7/3〜7/7の親QR/claim挙動テストの残骸（テスト垢3つ）。
  本番想定の「100枚フルclaim」実データはまだ無い→公開前に1束フルclaimで
  手持ちタブの見え方を実測すること。
- 未claim在庫の健全性確認: 束886ec09c(7/10)の10枚=id261〜270は claimed_at NULL・
  short_id 全て有り。手持ちに出ず正常。short_idは束生成時に付与されている（実証）。
- short_id NULL（id234・238）= 仕様と確定（バグではない）:
  白紙claimのRPC reset_blank_and_claim が「旧部屋のshort_idをNULL化し、同じshort_idで
  白紙の新部屋を立てて付け替える」動作。234・238は付け替え元の"抜け殻"。
  ※副作用: 白紙束をclaimするほど「short_id NULLの抜け殻部屋」が増える。実害なし
  ※**reply_and_claim も同型の抜け殻を作る**（2026-08-03確定）。id277（#257）が実例
- draft+message併存22件（2026-07-22実測）: 全件 draft1/message1 で固定。
  現行card.htmlは確定時にdraftをdelete→messageをinsertするため今後は増えない。
  ※低確率の穴: delete と insert が別await・非トランザクション。実害微小
- global_no二重: **68(0ei51/edk9ht)**・69(id79/89)・71(id81/91)・72(id82/92)。
  **68は2026-08-03に追加発見**。lot_no導入＋mypage表示改修と同時に解消想定
- 会員（profiles 4件・2026-07-22実測）: 全員カード保有者と一致・幽霊なし。
  name列は全NULL（pen_nameはauth.users側のuser_metadata）
- **lot_no が NULL の部屋は30件（2026-08-07実測）**:
  ・うち**修復できたのは id287（#267）の1件のみ**＝H0002-002 を埋め戻した
  ・**残る29件は id202〜248 の7月上旬テスト残骸**。lot_no 列を作る前に生まれたため
    同じ束・同じ position の兄弟部屋にも lot_no が存在せず、**埋めるべき値が無い**。
    正しいNULLであり修復対象ではない
  ・辿り方は **bundle_id + position** で兄弟部屋から引く（room_transfers 経由で
    to_user を突き合わせると総当たりになって1部屋が12行に膨れる。使わないこと）
- **H0002束10枚の状態（2026-08-03実測／2026-08-07に表示仕様が変更）**:

| lot_no | global_no | short_id | 状態 | print.htmlでの見え方 |
|---|---|---|---|---|
| H0002-001 | 256 | 4yoftg | 確定済み・未claim | 黒地「使用済み」 |
| H0002-002 | 257→**267** | **NULL**（267へ付替） | **世代交代済み** | **黒地「使用済み」**（旧:白紙） |
| H0002-003 | 258 | qyouez | 未確定 | 通常表示 |
| H0002-004 | 259 | nol8om | 確定・お役目終了 | 黒地「使用済み」 |
| H0002-005〜010 | 260〜265 | 各有り | 未確定 | 通常表示 |

  ・**002の白紙は2026-08-07に解消**（次の一手16）。現在は001・004と完全に同じ
    黒地「使用済み」で描かれ、小さい文字は lot_no のみ。
    **紙から claim の有無は読めない**
  ・紙のH0002-002（g666me）は生きており #267 を指す。持ち主は
    `5f9eeb8a…`（TAKAのテスト垢）。**これは所有権移動の実験として意図的に行ったもの**
  ・**#267 の lot_no は2026-08-07に H0002-002 を埋め戻し済み**
  ・**001も相手が返信すれば同じ黒表示のまま**＝既読は見えない（解消済み）
- **#256（H0002-001）の実態（2026-08-04実測）**: 知人に手渡し済み。
  `is_confirmed=true` ／ `short_id=4yoftg` ／ `owner_user_id` はTAKAのまま ／
  `closed_at=null`。**確定しただけで返信もclaimも起きていない**。
  お役目終了はまだ打てるが、**打てば受け手は返信も取り込みもできなくなる**。
  ※**受け手は未ログインで開いているがログには1件も無い**＝取りこぼしの実例。
    **QR16mmの読取自体は成功している**
- **#259（H0002-004）の実態（2026-08-03実測）**: room_posts は message 1件のみで
  **reply なし**＝相手は読んだだけ。claim も起きておらず、
  相手のマイページに出ないのが正しい。
  時系列は 15:07:15 確定 → 15:07:38 相手が開く → 15:23 お役目終了 →
  15:25:59 相手が再度開く。**閉じた後も受け手はQRから読めている**
- **#268（co6k88・URL型・妹へLINEで送付）のログ（2026-08-07実測）**:
  ・全7件＋当日5件。**未ログインが計6件記録されている**＝
    URL型は未ログインでもログが入る
  ・2026-08-03 06:45 の1件は **iPhone OS 18_7 + LINE内蔵ブラウザ**
  ・2026-08-07 14:23〜14:36 の5件は **Android 16 / SCG24 + LINE内蔵ブラウザ**
    ＝妹本人。**8/03の iPhone とは別端末**で、8/03分の主が不明のまま残る
  ・5件は14分間に間隔1〜6分でバラバラ＝画面の行き来。**1人で5件出る**
  ・**妹がログイン後の記録は入っていない**。ログイン後は receive.html を
    経由していない可能性（未検証）
- **claim は全期間で1回のみ**（2026-08-07時点）。#257→#267 だけ。
  「保存確定カード」に複数並ぶのは回数ではなく**残すべきパターンの数**

## 保存確定カード（大掃除で消さない）
※回した回数ではなく、**残すべきパターンごとに1枚ずつ**選んでいる。
- gxpp83（#187/印刷屋に試し渡し）: message1件・画像のみ・往復なし。ログ無し
- a79im9（#188/居酒屋に試し渡し）: message1件・画像のみ・往復なし。
  7/20夜(JST)に非ログインAndroidで開かれた記録あり
- #201（global_no201/URL型/owner=korokoro）
- **#257→#267（g666me／紙はH0002-002）**: **claimが起きた唯一の記録**（所有権移動の実験）。
  G666バグと白紙表示の検証にも使うため残す。
  ※両バグとも2026-08-07に修正済み。**修正が効いていることの確認用として引き続き残す**
- **#256（4yoftg／紙はH0002-001）**: 知人へ手渡し。確定済み・返信待ち・未claim。
  **ログの取りこぼしを発見した実例**。次の一手12の修正後の検証にも使う
- **#259（nol8om／紙はH0002-004）**: 確定→相手が読んだ→返信なしで閉じた、という
  実運用パターンの記録。閉じた後の閲覧も記録されている
- **#268（co6k88／URL型・妹へLINE送付）**: **URL型で外部の人が実際に読んだ初の記録**。
  未ログイン閲覧が正しく記録されること、1人が複数件を出すこと、
  LINE内蔵ブラウザのUAが取れることが確認できたサンプル

## 検討中テーマ（別チャットで進行）
- 協賛100枚バッチ作成（起票2026-07-22）: 束claim後に白紙が手持ちへバラ並びする問題を、
  「ワンクッションの一括作成ページで100枚に同一内容を焼き込む」方向で検討。
  複製案/一括作成案の比較・個別差替可否・焼込後の編集可否は別チャットで詰める
- クウの羽織デザイン（起票2026-07-24）: 上記「mypage実機宿題／デザイン段階」参照
- 家庭用カードのデザイン（起票2026-07-28）: **2026-08-02に一巡完了**。
  残るは print.html の両面化改修（次の一手8）。
  確定事項は docs/家庭用カード仕様_2026-07-29.md
- 個人名入りカード構想（起票2026-08-02）: **着手しない・需要が出てから**。
  詳細は docs/個人名入りカード構想_2026-08-02.md
- **閉じたカードを受け手が持てるようにするか（起票2026-08-02・TAKA意見「持てるべき」）**:
  お役目終了したカードは、受け手はQRから中身を読めるが**マイページには取り込めない**。
  紙を持っていて中身も読めるのに記録に残らないのは受け手から見て不自然、という論点。
  一方で「返信が唯一の応答」という設計の骨格に触る。
  ※**2026-08-03に実データで発生を確認**（#259）。閉じた2分後に受け手が開いている
  ※運用上の注意: **確定済み・未返信のカードを閉じると、受け手の応答手段が消える**
- **お役目終了の権限設計（起票2026-08-02）**:
  確定後は**紙が受け手の手にあるのに権限は送り主に残る**という非対称が生じる。
  返信後は所有権が移るため問題は「確定・未返信」の区間だけ。当面は現状維持
- **番号の履歴を残す（起票2026-08-04・TAKA発案）**:
  運営側の履歴管理として、**その時々の global_no × short_id × lot_no の対応**を
  残したい。現状は claim のたびに global_no が変わり、short_id が部屋を移るため、
  **紙と部屋の対応が追えなくなる**。
  ※**2026-08-07の lot_no 引き継ぎ修正により、状況は改善した**。
    claim後も新部屋が lot_no を持つため、紙→部屋の対応は lot_no で辿れる。
    ただし「いつ移ったか」の時系列は room_transfers を見る必要がある点は変わらない
  案＝room_transfers に「いつ・どの紙が・どの部屋からどの部屋へ」を1行残す。
  設計の話なので別チャットでテーマ立てる
- **`#` 表記を `No.` にするか（起票2026-08-04・見栄えの話）**: **今は保留**。
  DBは触らず表示だけの変更で可能。懸念＝①小さい文字では `#267` のほうが視認性が高い
  ②lot_no は `H0002-002` 形式のため並べると表記がちぐはぐになる
- **「感謝」語彙をどこまで「こころ」に寄せるか（起票2026-08-02）**:
  統計ラベルは「届けた／受けとった」に変更済み。他に残る表現は未判断
- 運営ダッシュボード／数字の把握（起票2026-08-02／2026-08-12にdocsへ集約）:
  SQLの読み方・保存クエリ・実測値・拡張候補・プラポリ要件は
  **docs/運営ダッシュボード_2026-08-12.md** に一本化。
  ページ表示回数はDBで取れない（Cloudflare Web Analytics が候補）点も同docsに収録
- **LINE内蔵ブラウザのセッション分断（起票2026-08-12）**: LINEでURLを渡すと
  受け手は常に未ログイン扱いになり**返信もclaimも通らない**。実データで確認済み。
  URL型の配布経路に関わるため別テーマで検討（詳細＝docs/運営ダッシュボード_2026-08-12.md）
- **HANDOFF.md の分割（起票2026-08-04・2026-08-07に順序を再検討）**: 読む負荷が高い。
  案＝①HANDOFF本体は「今どこ＋次の一手＋docs/へのポインタ」に絞る
  ②3本のdocsへ分割する（開く目的が違うため）:
    ✅docs/運営ダッシュボード_2026-08-12.md（2026-08-12作成・1本目完了）
    docs/バグと観測記録_yyyy-mm-dd.md ← 実機宿題の【バグ・要再現】＋DB観測メモ＋保存確定カード
    docs/mypage改善候補_yyyy-mm-dd.md ← 【機能追加】【デザイン段階】【共通UI・ナビ】
  ③各docsの先頭に現在地を書く
  ・**解決済みバグも捨てずにdocsへ運ぶ**（「なぜそうなったか」が残らないと同じ穴を踏む）。
    ただし「解決済み」と明記して現役バグと分ける
  ・**実施タイミングは印刷の後**（2026-08-07判断）。減量は印刷の期日に効かないため、
    A-5/A-6（両面化→実刷り）を通してから、増えた記録ごとまとめて整理する

## リポジトリ内ファイル（本番HTML以外）
- BACKUP.md … バックアップ手順
- .nojekyll … Pagesビルド設定（中身空で正常・削除禁止）
- favicon.ico / apple-touch-icon.png / icon-192.png / icon-512.png …
  サイトアイコン。**2026-08-07にファイル名を修正済み**（旧・壊れた名前は削除）。
  ※4点とも中身は同一画像（26,958バイト）。favicon.ico の中身はPNG。
    クウの絵ができたら正規のico・各サイズPNGで作り直す
- sql/discard_card_backup_20260711.sql … discard_card原本
- sql/migration_20260705_rls.sql … RLS定義原本
- fonts_subset.js … カード文言専用のフォントサブセット。**文言を変えたら再生成必須**
- docs/ … テーマ別の正MD
  - docs/言葉ストック_2026-07-24.md … 表題・キャッチコピー・語彙の貯金箱
  - docs/家庭用カード仕様_2026-07-29.md … 家庭用の確定仕様
  - docs/個人名入りカード構想_2026-08-02.md … 併記レーンの構想（着手しない）
  - docs/運営ダッシュボード_2026-08-12.md … 数字の見方・保存クエリ・ログ仕様の正
  - docs/決定台帳.md … 設計判断の思想履歴（未作成・第1章=番号体系、
    第2章=ドメイン、第3章=ポイント重み付け1:2）

## 書いたらあかんもの（要点）
鍵・トークン・parent_token・ドメイン認証鍵(AuthCode)・未修正脆弱性の詳細・
個人情報・short_idと人物の対応・SMTPのパスワード/APIキー
※Web3FormsのAccess Keyは公開前提の値のため該当しない（HTML直書きで正）
