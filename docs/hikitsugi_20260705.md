# 引き継ぎメモ（2026-07-05 実装チャット完了分）

位置づけ：「カード状態遷移 統合メモ（2026-07-04）」の実装を本番投入した日の記録。
次チャットの冒頭に貼る用。

---

## 今日完了（本番稼働中）

**SQL（Supabase投入済み）：**
- claim三経路の実装：白紙claim（claim_blank_card）／親QR一括claim（claim_bundle v2＋reset_blank_and_claim）／返信＝claim（reply_and_claim）
- 供養＝「お役目終了」方式：rooms.closed_at追加。short_idは残す→再スキャンで「お役目を終えました」表示。番号再利用なし
- discard_card v3：権限＝カードの現所有者（rooms.user_id）。レガシー返信者ルートも救済で残存
- reply_and_claim v2：バトン移動はroom_type='qr'のみ。URL部屋は返信のみで完結
- delete_room新設：未確定のURL部屋を持ち主が削除可（QRは削除不可→お役目終了で閉じる）
- generate_short_id防御修正（明示short_idを上書きしない）
- global_noシーケンス同期＋手動採番の全廃（nextval統一）
- bundles.claimed_by/claimed_at追加（親QR一度きりロック）

**フロント（GitHub main反映済み・デプロイ成功確認済み）：**
- receive.html：白紙確認画面（明示ボタンclaim）／お役目終了表示／返信＝claim統合／再利用UI撤去
- claim.html新設：親QR一括claim（claim.html?b=親トークン）。スキップ表示＋現物確認UI＋二段確認上書き
- card.html：「QR名刺のお役目を終える」ボタン（QR現物持ちオーナーのみ）＋終了バナー
- mypage.html：由来バッジ（QR/URL）／URL未確定に削除ボタン／QRに「お役目終了」ボタン＋「お役目済」札／10件ページング（送った・もらった両方）
- auth.html：ログイン後の着地＝マイページ（?return=導線は無傷）
- print.html：room_type='qr'付与＋手動採番撤去（※旧プロト扱い。将来bundle方式の発行ページに置き換え予定）

**データ修正済み：**
- A4テスト分 #193〜#202（short_id保持9枚）→ room_type='qr' に更新済み

**検証クリア：**
- ★claim_room付け替え（統合メモ最優先）→ 返信claim→世代交代→再スキャンで新部屋、実機確認済み
- own_cardガード → self-transfer記録0件。破られてない（クローズ）
- ログイン後マイページ着地・由来バッジ表示・お役目終了ボタン → 動作確認済み

---

## 実装済み・未テスト

- 経路A（白紙claim確認画面）＋経路B（親QR一括claim・現物確認UI）
  → テストにはcreate_bundle手動実行でテスト束を作る必要あり。手順は実装チャットで誘導してもらう
  → 親QRのURL形式：claim.html?b=<parent_token>

---

## バックログ（2026-07-05 TAKA積み）

0. UX細かい改善（あとでいい・2026-07-05積み増し）：
   - マイページ：カードを1つ開いて戻るとページングが1ページ目に戻る → ページ位置の保持
   - カード表示に「次へ／前へ」で連続閲覧できてもいいかも
   - card.html：返信が来て完結しても一番下が「心が届きますように。」のまま → 完結時は文言を変える
1. お役目終了カードの表示改善：一覧から露出なし・No.のみ・部屋は開ける、等（今度でいい）
2. 穴の部分（残る設計・セキュリティの隙の総点検）
3. カードのNo.表示体系：グローバルNo.／ロット＋連番（Lot.0001-001）→ 印刷本番化時に確定
4. 家庭用印刷リンク
5. スポンサー用印刷
6. スポンサー仮設定（contractsテーブル・receive時参照・期限後は無名の神）
7. アカウントの種類：特に運営側・スポンサー側の区分
8. 祈りの木 → 優先度下げる
9. 期限バッチ（バトン2ヶ月＋完走猶予1ヶ月・pg_cron）→ 本番ユーザー乗せ前でよい
10. テストカード整理【延期確定】：実施は本番ユーザー乗せ直前の大掃除で。残すのは **#15（short_id: dby5db・保全対象の完結カード）**＋QR10枚系譜（#193〜#202＋バトン子孫）。それ以外のテストカードは削除対象。削除SQLはその時に作成（焼くは不可逆）

---

## 技術メモ（事故防止）

- global_noは絶対に手動で入れない（DBのシーケンスに任せる）。過去の二重採番（#68〜#75の重複）は放置でOK
- 焼くは不可逆：削除・番号体系・QR印刷は最後に確定
- デプロイ失敗メール：連続コミット時に途中のrunが失敗扱いになるだけ。Actionsで最新runが緑なら問題なし
- チャット内A4版印刷HTMLは番号体系確定までコミットしない（現役はDB上の部屋＋チャット内HTML）
